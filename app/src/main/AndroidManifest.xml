<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.mamode.anthony.mynews">

    <!-- Permissions -->
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- todo: fix bad google indexation -->
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".controllers.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".controllers.NewsWebView"/>

        <!--Child of MainActivity-->
        <activity
            android:name=".controllers.NotificationsActivity"
            android:label="@string/notification_activity_name"
            android:parentActivityName=".controllers.MainActivity">
            <!-- The meta-data element is needed for versions lower than 4.1 -->
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".controllers.MainActivity"/>
        </activity>
        <!--Child of MainActivity-->
        <activity
            android:name=".controllers.SearchActivity"
            android:label="@string/search_activity_name"
            android:parentActivityName=".controllers.MainActivity"
            >
            <!-- The meta-data element is needed for versions lower than 4.1 -->
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".controllers.MainActivity"/>
        </activity>
    </application>

</manifest>